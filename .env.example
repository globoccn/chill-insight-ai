VITE_DASHBOARD_DATA_URL=/api/dashboard
VITE_N8N_WEBHOOK_URL=https://ancar-n8n.gpfgqx.easypanel.host/webhook/dados-globo-vm22

# Necessário para a aba Settings salvar em Redis via REST.
# Use Upstash Redis REST ou outro gateway HTTP compatível com comandos Redis.
# Se você usa Redis TCP comum no EasyPanel, crie um pequeno gateway REST ou faça o n8n receber / salvar cag:settings.
REDIS_REST_URL=
REDIS_REST_TOKEN=
